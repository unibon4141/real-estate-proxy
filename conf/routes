GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

GET    /proxy/                     controllers.HomeController.proxy(path: String, qNames: List[String], qVals: List[String])
